{
  "name": "ai-smart-food-redistribution",
  "version": "1.0.0",
  "description": "Smart Food Redistribution Project",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:ai": "cd ai && python app.py",
    "build": "cd frontend && npm install && npm run build",
    "prepare:prod": "node scripts/prepare-prod.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
